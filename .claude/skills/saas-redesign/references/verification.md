# Verification — saas-redesign

How to capture before/after screenshots and build the comparison gallery. Used in Phase 2 and Phase 6 of the skill.

## Screenshot routine

For each of the six routes, do this in sequence:

1. `mcp__playwright__browser_navigate` → `http://localhost:3000<route>`
2. `mcp__playwright__browser_resize` → `{ width: 1440, height: 900 }` (only needed once per session)
3. `mcp__playwright__browser_wait_for` → wait for a known page-header string for that route (e.g. text: "Dashboard" for `/`, "Inventory" for `/inventory`). If the i18n locale is `en`, header strings match `t('nav.<key>')` values in `client/src/locales/en.js`.
4. `mcp__playwright__browser_take_screenshot` → save to `docs/redesign/<prefix>-<slug>.png`, full-page true.

Route → slug map:

| Route | Slug | Wait-for text (en) |
|-------|------|--------------------|
| `/` | `overview` | "Overview" or "Dashboard" |
| `/inventory` | `inventory` | "Inventory" |
| `/orders` | `orders` | "Orders" |
| `/demand` | `demand` | "Demand" |
| `/spending` | `spending` | "Finance" or "Spending" |
| `/reports` | `reports` | "Reports" |

Confirm the wait-for strings against the running app before assuming — they live in `client/src/views/<View>.vue` page headers.

## Gallery HTML

Write this file at `docs/redesign/index.html` in Phase 6, after all screenshots exist.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<title>SaaS Redesign — Before / After</title>
<style>
  :root {
    --bg: #f8fafc; --surface: #ffffff; --ink: #0f172a; --muted: #64748b;
    --line: #e2e8f0; --accent: #2563eb;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0; background: var(--bg); color: var(--ink);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Inter, sans-serif;
  }
  .wrap { max-width: 1600px; margin: 0 auto; padding: 56px 32px; }
  h1 { margin: 0 0 8px; font-size: 32px; letter-spacing: -0.02em; }
  p.lede { margin: 0 0 48px; color: var(--muted); }
  .row {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 14px;
    margin-bottom: 32px;
    overflow: hidden;
  }
  .row h2 {
    margin: 0;
    padding: 20px 24px;
    font-size: 16px;
    font-weight: 600;
    border-bottom: 1px solid var(--line);
    background: #f1f5f9;
  }
  .pair { display: grid; grid-template-columns: 1fr 1fr; }
  figure { margin: 0; padding: 24px; border-right: 1px solid var(--line); }
  figure:last-child { border-right: 0; }
  figcaption {
    font-size: 11px; text-transform: uppercase; letter-spacing: 0.08em;
    color: var(--muted); margin-bottom: 12px; font-weight: 600;
  }
  img { width: 100%; height: auto; display: block; border: 1px solid var(--line); border-radius: 10px; }
</style>
</head>
<body>
<div class="wrap">
  <h1>SaaS Redesign — Before / After</h1>
  <p class="lede">Generated by the <code>saas-redesign</code> skill. Six routes, captured at 1440×900.</p>

  <!-- Repeat per route -->
  <section class="row">
    <h2>Overview · /</h2>
    <div class="pair">
      <figure><figcaption>Before</figcaption><img src="before-overview.png" alt="Overview before"></figure>
      <figure><figcaption>After</figcaption> <img src="after-overview.png"  alt="Overview after"></figure>
    </div>
  </section>
  <!-- … inventory, orders, demand, spending, reports … -->
</div>
</body>
</html>
```

When generating the gallery, the skill writes one `<section class="row">` per route, in the same order as the route table above. If a screenshot is missing (e.g., capture failed on one route), the gallery should still render — leave the `<img>` with an `onerror` fallback that shows a "[missing]" label, and tell the user which route failed.

## Open the gallery

```bash
open "/Users/shaanjain/Anthropic Basecamp/inventory-management/docs/redesign/index.html"
```

## Console-error check

After capturing "after" screenshots, before declaring success:

1. `mcp__playwright__browser_navigate` → `/`
2. `mcp__playwright__browser_console_messages` → fetch all messages
3. Filter for level `error` — if any non-empty, report them to the user and *do not* mark Phase 6 complete.

## Filter-state preservation check

Manual but quick:

1. Navigate to `/`, change Time Period filter to a non-default value.
2. Navigate to `/orders`.
3. Inspect the `FilterBar` — the time period selector should still show the same non-default value.

If this regresses, `useFilters()` was likely re-instantiated somewhere — stop and tell the user.
